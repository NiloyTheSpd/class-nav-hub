import { PageContainer } from "@/components/PageContainer";

const Sessions = () => {
  return (
    <PageContainer title="Sessions">
      <p className="text-muted-foreground">Sessions content will be displayed here.</p>
    </PageContainer>
  );
};

export default Sessions;
