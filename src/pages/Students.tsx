import { PageContainer } from "@/components/PageContainer";

const Students = () => {
  return (
    <PageContainer title="Students">
      <p className="text-muted-foreground">Students content will be displayed here.</p>
    </PageContainer>
  );
};

export default Students;
