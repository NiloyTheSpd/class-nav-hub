import { PageContainer } from "@/components/PageContainer";

const Calendar = () => {
  return (
    <PageContainer title="Calendar">
      <p className="text-muted-foreground">Calendar content will be displayed here.</p>
    </PageContainer>
  );
};

export default Calendar;
